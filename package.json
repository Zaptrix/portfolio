{
  "name": "don-cuddihee-portfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Portfolio for software, AI systems, technical support, networking, and visual design.",
  "scripts": {
    "start": "python -m http.server 8080",
    "dev": "python -m http.server 8080",
    "build:scene": "esbuild src/js/scene-source.js --bundle --minify --format=iife --target=es2020 --outfile=src/js/scene.js",
    "build": "npm run build:scene",
    "check": "node --check src/js/app.js && node --check src/js/scene-source.js && npm run build:scene"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "esbuild": "0.25.6"
  }
}
